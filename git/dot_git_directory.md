# .git directory contains all history and config 
# brain of your git project
# .git/refs/ git stores tags and branches (stores branch switches here)
# .git/objects all your commits,blobs,trees. all compressed and stored by sha hash , can be considered projects hard drive
# ./git/head tells git which branch or commit your currently on
# .git/index this is the staging area for your commits
# ./git/config stores repo seicifci settings

