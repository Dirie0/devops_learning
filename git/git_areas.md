# workiing directory 
# where you actively edit your files, make the changes here

# staging area(index) put files that you're preparing to commit, things to be included in the snap


# repository local.git the snapshots are commit

# working directory -> git add(staging area) -> git commit -> repository (snapshots commited)

