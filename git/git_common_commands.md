# git init - initialises a brand new git repo with .git directory. 
# git add - stages your changes in the staging area(index)
# git commit -  takes a snapshot of the changes in the codebase
# git status - shows the staged and unstage d work
# git log sohws the git commit history
# git diff shows what changed 


# git config set user/email
# git help built in docs
# git clone copy a remote repo
# git rm remove files
# git mv rename files
# git restore undo fiel changes 