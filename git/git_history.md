# git log  - see commit history, useful when you want to walkback recent changes
# git log --oneline --graph visual summary . usefukl for seeing merge changes or release branch

# git show <commit>. view a specific commit

# git diff: compare unstaged vs last commit

# git diff --staged compare staged vs last commit (heres whats about to be commited)

